function calcular() {
    let valor1 = parseInt(prompt("Por favor, informe um número inteiro: "));
    let valor2 = parseInt(prompt("Por fim, informe mais um número inteiro: "));

    let resultado = (valor1 + valor2);

    alert(`O resultado é: ${resultado}`);
}